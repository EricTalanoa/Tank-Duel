# Task 6 Feel Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add configurable feedback effects, reduced-motion behavior, animated terrain collapse, synthesized audio, and frame-budget verification.

**Architecture:** Simulation emits semantic presentation events while render-only systems consume them. All tuning comes from `spec/effects.json`; collapse remains deterministic simulation work limited to dirty columns.

**Tech Stack:** TypeScript 7, Vitest 4, Vite 8, Canvas 2D, Web Audio

**Spec:** `docs/superpowers/specs/2026-08-27-task-6-feel-design.md`

## Global Constraints

- Import every effect value from `spec/effects.json`; do not duplicate documentation numbers in code.
- Do not modify golden vectors or consume simulation RNG for rendering.
- Reduced motion disables shake/hitstop, uses 25% particles, and preserves trajectories.
- Stop at Task 6's `Stop here` line; Task 7 is out of scope.
- This directory has no Git metadata, so commit steps are omitted.

---

### Task 1: Effects Configuration

**Files:** Create `spec/effects.json`; create `src/render/effectConfig.ts`; create `src/render/effectConfig.test.ts`.

**Produces:** typed `EFFECTS` with hitstop, collapse, reduced-motion, performance, particle, shake, flash, and audio sections.

- [ ] Write a failing test importing `EFFECTS` and asserting structural validation rejects non-positive caps/durations and fractions outside 0..1.
- [ ] Run `npm test -- src/render/effectConfig.test.ts`; expect missing-module RED.
- [ ] Add `spec/effects.json`, typed config import, and `validateEffectsConfig`.
- [ ] Rerun focused test and full suite; expect GREEN.

### Task 2: Motion Policy

**Files:** Create `src/render/motion.ts`; create `src/render/motion.test.ts`.

**Produces:** `motionPolicy(reduced): { shake; hitstop; particleMultiplier; trajectories }`.

- [ ] Write failing normal/reduced policy tests using only imported `EFFECTS` values.
- [ ] Verify RED, implement the pure policy, verify GREEN.

### Task 3: Presentation Events and Direct Hits

**Files:** Create `src/sim/presentation.ts`; create `src/sim/presentation.test.ts`; modify `src/sim/world.ts`.

**Produces:** `PresentationEvent`, `presentationEvents`, `drainPresentationEvents`, and `pointInHull`.

- [ ] Write failing tests proving fire emits one muzzle event, resolve emits impact events, and an impact in either half-open hull box emits one direct-hit event.
- [ ] Verify RED.
- [ ] Implement the queue and geometry using tank constants, then emit events from successful fire and shared resolve.
- [ ] Verify focused and full suites GREEN.

### Task 4: Deterministic Visual Effects Engine — Midpoint Stop

**Files:** Create `src/render/effects.ts`; create `src/render/effects.test.ts`.

**Produces:** `createEffects(seed, policy)`, pooled particles, flash, shake, hitstop counters, `consume`, `update`, and `draw`.

- [ ] Write failing tests for deterministic particles, configured particle bounds, reduced count, zero reduced-motion shake/hitstop, and exact normal hitstop frames.
- [ ] Verify RED.
- [ ] Implement render RNG independent of sim RNG and preallocated particle pooling.
- [ ] Verify focused/full tests and strict build GREEN.
- [ ] Stop here for the requested half-task checkpoint.

### Task 5: Dirty-Column Terrain Collapse

**Files:** Create `src/sim/collapse.ts`; create `src/sim/collapse.test.ts`; modify `src/sim/world.ts`.

**Produces:** queued column ranges and `stepCollapse` moving solids by at most `EFFECTS.collapse.maxPixelsPerFrame`.

- [ ] Test only queued columns are visited and movement is capped.
- [ ] Integrate collapse with RESOLVE dirty ranges and SETTLE quiet detection.
- [ ] Verify deterministic termination and full suite.

### Task 6: Synthesized Audio

**Files:** Create `src/render/audio.ts`; create `src/render/audio.test.ts`.

**Produces:** safe `unlock`, `playFire`, `playImpact`, and `playDirectHit` APIs.

- [ ] Test unavailable/suspended audio paths as safe no-ops through an injected audio-context factory.
- [ ] Implement spec-driven oscillator/noise envelopes and verify GREEN.

### Task 7: Main Loop and Renderer Integration

**Files:** Modify `src/main.ts`, `src/render/renderer.ts`, `src/render/entities.ts`, and input setup.

- [ ] Add failing hitstop-step-controller tests proving nine normal frames consume no sim steps while reduced motion continues stepping.
- [ ] Drain events once per frame, update effects, apply world-only shake, render particles/flash, unlock audio on interaction, and listen for media-query changes.
- [ ] Verify trajectories continue under reduced motion and full suite/build GREEN.

### Task 8: Performance and Browser Acceptance

**Files:** Create `src/render/effects.performance.test.ts`; modify tests only if acceptance gaps are found.

- [ ] Benchmark the spec-selected mortar blast after warmup and assert under `EFFECTS.performance.frameBudgetMs`.
- [ ] Audit all three Task 6 assertions line by line.
- [ ] Run fresh full tests and build.
- [ ] Browser-test normal and emulated reduced motion, effects, collapse, audio initialization, frame timings, and console cleanliness.
- [ ] Stop at Task 6's `Stop here` line.

